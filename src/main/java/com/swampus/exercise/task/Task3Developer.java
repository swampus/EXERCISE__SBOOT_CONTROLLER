package com.swampus.exercise.task;

import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

/**
 * Create a controller that consumes GET: /task3/getKey({long}) like /task3/getKey(1) or /task3/getKey(2)
 * Return Key (Class already done) and assign to field x (Key.x) number that is received from URL
 *
 */
@RestController
@RequestMapping("/task3")
public class Task3Developer {

}
