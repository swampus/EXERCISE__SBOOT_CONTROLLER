package com.swampus.exercise.task;

import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

/**
 * Create a method that listening POST: /task4/num_holder
 * as params ( @RequestBody ) it gets Class NumberHolder (already created)
 * find a sum of fields (a, b, c.) method should return value of a sum)
 */

@RestController
@RequestMapping("/task4")
public class Task4Developer {

}
